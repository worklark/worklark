<%- if controller_name != 'sessions' %>
  Already have an account? <a href="<%= new_user_session_path %>">Log in</a>.<br>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  Don't have an account? <a href="<%= new_user_registration_path %>">Sign up</a>.<br>
<% end %>

<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  Forgot password? Click <a href="<%= new_password_path(resource_name) %>">here</a> to reset.<br>
<% end %>

<%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
  Resend <a href="<%= new_user_confirmation_path %>">confirmation</a> or
<% end %>

<%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <a href="<%= user_unlock_path %>">unlock</a> email.
<% end %>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider) %><br />
  <% end %>
<% end %>
